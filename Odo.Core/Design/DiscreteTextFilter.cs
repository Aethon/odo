using System.Collections;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace Odo.Core.Design
{
    /*
    public class CategoryFilter<T> : CategoryFilter
    {
        public new Binding<IEnumerable<Category<T>>> Categories { get { return (Binding<IEnumerable<Category<T>>>) base.Categories; } }

        internal CategoryFilter(Binding context,
            string name,
            Binding<string> style,
            Binding<IEnumerable<Category<T>>> categories,
            DesignTemplate displayTemplate)
            : base(context, name, style)
        {}
    }*/
}
